# change prefix
set-option -g prefix C-x
bind C-x send-prefix
unbind-key C-b

# status
set -g status-style fg=black,bg=white
set -g display-time 3000

# command/message line colors
set -g message-style fg=black,bg=white,bright
set -g pane-border-style fg=white
set -g pane-active-border-style fg=white
# set -g pane-active-border-bg black

set-window-option -g mode-style fg=black,bg=white

# select pane on mouse
set-option -g mouse on

# sync clipboard
# for linux
if "which xclip" "bind-key -T copy-mode M-w send -X copy-pipe-and-cancel 'xclip -i -selection clipboard'"
if "which xclip" "bind-key -n C-y run 'tmux set-buffer -- \"$(xclip -o -selection clipboard)\"; tmux paste-buffer'"

#for mac
if "which pbcopy" "bind-key -t emacs-copy M-w copy-pipe 'pbcopy'"
if "which pbpaste" "bind-key -n C-y run 'tmux set-buffer -- \"$(pbpaste)\"; tmux paste-buffer'"

# split
bind-key h split-window -h
bind-key v split-window

# remove some key
unbind-key {
unbind-key }
unbind-key C-o

# set-option -g utf8-cjk on
